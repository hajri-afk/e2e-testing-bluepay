import { test, expect } from '@playwright/test';

/**
 * Contoh test yang menggunakan authentication state
 * Login sudah dilakukan sekali di global-setup.ts
 * Test ini langsung menggunakan session yang sudah login
 */
